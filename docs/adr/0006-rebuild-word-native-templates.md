# Rebuild clean Word-native templates

The replacement workflow will derive clean Word-native templates from the Reference Protocol’s design system instead of mutating the current DOCX packages. Real Word styles, sections, lists, tables, headers, footers, and fields trade initial rebuild effort for predictable Microsoft Word rendering and localized formatting rules.
