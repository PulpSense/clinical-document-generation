# Approve repeated document data before generation

Visits, schedules, endpoints, arms, interventions, sites, and contacts that feed repeated document structures are represented as structured rows in the Source-of-Truth Markdown before approval. Drafting agents may narrate those rows but do not reconstruct critical tables from ambiguous prose after approval, trading a more explicit review surface for reliable DOCX tables and cross-document consistency.
