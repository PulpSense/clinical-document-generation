interface:
  display_name: "Clinical Document Generation"
  short_description: "Review and generate study documents"
  default_prompt: "Use $clinical-document-generation to prepare a study document set, asking only for missing or conflicting starred Fillout fields in every study branch before source review."
